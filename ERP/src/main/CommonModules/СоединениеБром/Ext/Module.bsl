Функция ПолучитьОсновнойПрайсЛистНаДату(ДатаСреза=Неопределено, План=Неопределено) Экспорт
	
	Если ДатаСреза = Неопределено Тогда
		
		ДатаСреза = ТекущаяДата();	
		
	КонецЕсли;
	
	Если План = Неопределено Тогда
		
		План = Перечисления.ПланПодписки.Эконом;	
		
	КонецЕсли;

	Возврат РегистрыСведений.ИсторияИзмененийОсновныхПрайсЛистов.ПолучитьОсновнойПрайсЛистНаДату(ДатаСреза, План);
	
КонецФункции